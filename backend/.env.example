# 服务器配置
NODE_ENV=development
PORT=3000
HOST=localhost

# 数据库配置
DB_HOST=localhost
DB_PORT=3307
DB_NAME=library_management
DB_USER=root
DB_PASSWORD=root
DB_DIALECT=mysql

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis123456
REDIS_DB=0

# JWT配置
JWT_SECRET=your_jwt_secret_key_here_change_in_production
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# 初始管理员密码（仅建议开发环境使用；生产环境请务必修改/通过安全渠道注入）
INIT_ADMIN_PASSWORD=admin123

# 微信小程序配置
WECHAT_APP_ID=your_wechat_app_id
WECHAT_APP_SECRET=your_wechat_app_secret

# 文件上传配置
UPLOAD_PATH=./public/uploads
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf,doc,docx


# 积分系统配置
POINTS_BORROW_BOOK=10
POINTS_RETURN_ON_TIME=5
POINTS_WRITE_REVIEW=25
POINTS_COMPLETE_TUTORIAL=50

# API限流配置
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 日志配置
LOG_LEVEL=info
LOG_FILE_PATH=./logs

# CORS配置
CORS_ORIGIN=http://localhost:8080,http://localhost:3001

# 开发环境可选：允许任意 localhost/127.0.0.1 端口（例如 Vite 随机端口）
CORS_ALLOW_ALL_LOCALHOST=false

# SSL配置 (生产环境)
SSL_KEY_PATH=
SSL_CERT_PATH=

# 其他安全配置
BCRYPT_SALT_ROUNDS=12
SESSION_SECRET=your_session_secret_here

# PDF阅读器配置
PDF_VIEWER_PATH=./public/pdf-viewer
EBOOK_STORAGE_PATH=./public/ebooks

# 数据库连接池配置
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_POOL_ACQUIRE=60000
DB_POOL_IDLE=10000
